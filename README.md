# User_Registration_System
A lightweight Python script that processes and validates user registrations. It cleans raw inputs, enforces strict rules for names, emails, and passwords, and prevents duplicate sign-ups. Valid accounts are stored cleanly, while failed attempts are captured in an audit log with failure reasons for easy tracking and debugging.
